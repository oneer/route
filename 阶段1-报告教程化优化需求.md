# 阶段 1：传统 ISP 与 RAW 基础报告教程化优化需求

## 任务目标

你是一名资深 ISP 算法工程师、计算摄影研究者和中文技术教材作者。请对 `stage1_soft_isp/` 进行系统审查、实验补强和教程化改造。

这不是简单润色或机械扩写 Markdown。最终目标是把阶段 1 建设成一套能够让学习者从 RAW/DNG 出发，完整理解并独立实现基础 RAW-to-RGB Soft-ISP 的中文教程，同时可以用于复习、实验复现、故障诊断和面试表达。

学习主线必须围绕：

```text
RAW/DNG
→ metadata 与 Bayer 数据检查
→ BLC
→ DPC
→ LSC
→ Demosaic
→ AWB
→ CCM
→ Tone Mapping
→ Gamma / sRGB OETF
→ IQA、消融与故障诊断
```

## 一、开始工作前必须审查

先完整阅读并建立对应关系，不要直接改报告：

- `stage1_soft_isp/README.md`
- `stage1_soft_isp/README_CN.md`（如存在）
- `stage1_soft_isp/reports/`
- `stage1_soft_isp/materials/`
- `stage1_soft_isp/exercises/`
- `stage1_soft_isp/soft_isp/`
- `stage1_soft_isp/scripts/`
- `stage1_soft_isp/tests/`
- `stage1_soft_isp/configs/default.yaml`
- `study-roadmap/阶段1-传统ISP与RAW基础学习路线.md`

重点检查：

- `reports/stage1_report.md`
- `reports/stage1_deep_review_improvement_plan.md`
- `reports/module_mastery_matrix.md`
- `reports/openisp_reference_notes.md`
- `reports/week1/` 至 `reports/week6/`
- `materials/stage1_start_here.md`
- `materials/module_study_template.md`
- `materials/debugging_guide.md`
- `exercises/final_project.md`

先形成“报告章节—代码实现—配置参数—运行脚本—实验图片/JSON—测试证据”对应表，并说明：

- 当前已完成什么；
- 哪些内容重复、过时或互相矛盾；
- 哪些章节只有结论，缺少推导、代码或实验；
- 哪些公式缺少变量、范围、单位和假设；
- 哪些结果没有解释“为什么”；
- 哪些不足能用现有数据补齐；
- 哪些必须依赖 ColorChecker、真实 flat-field、dark frame 等额外数据。

审查后直接实施修改，不要只提交建议。

## 二、事实与修改边界

1. 所有“本项目完成了什么、结果是多少”的陈述必须来自实际代码、配置、CSV/JSON、图片或重新运行的实验。
2. 禁止编造数据、运行结果和产品级实现。
3. 不得把 rawpy 称为 ground truth。
4. 不得把 OpenCV edge-aware demosaic 称为 AHD。
5. 不得把 synthetic flat-field 称为真实 LSC 标定。
6. 不得把相对 rawpy 的 DeltaE 称为标准 ColorChecker DeltaE。
7. 理论预期、文献结论和仓库实测必须明确区分。
8. 以报告教程化为主。只有为修正文档—代码不一致或补关键证据时，才做最小代码修改。
9. 不扩展阶段 2、3、4，不顺手重构无关代码。
10. 运行不了的实验要记录具体原因，不得假装验证成功。

## 三、统一教学标准

默认读者具备 Python、NumPy 和基础图像处理知识，但没有完整 RAW、Sensor、色彩科学和 ISP 体系。

每个术语首次出现必须解释。每个核心模块尽量包含：

1. 学习目标与先修知识；
2. 一句话定位；
3. 问题的物理来源；
4. Pipeline 位置及顺序原因；
5. 输入输出的域、shape、dtype、range、是否线性；
6. 直觉解释；
7. 核心假设；
8. 公式、变量和边界条件；
9. 可手算的小型数值例子；
10. 本项目算法流程；
11. 对应代码文件、函数和调用关系；
12. 参数的物理意义；
13. 参数增减的预期影响；
14. 运行命令和实验产物；
15. 如何阅读图、表、JSON 和差异图；
16. 结果证明了什么、没有证明什么；
17. 失败场景与典型伪影；
18. 上下游模块的误差传播；
19. 学习版、开源实现和产品级方法的差距；
20. 练习、自测题、参考答案和掌握标准；
21. 面试复述版本；
22. 延伸阅读及阅读目的。

讲解顺序优先采用：

```text
直觉 → 数学 → 小例子 → 代码 → 实验 → 失败案例 → 工程升级
```

总教程中增加完整数据域表，明确每一步的通道数、数值范围、是否线性、clip、归一化和 dtype 变化。

## 四、分周补强重点

### Week 1：RAW、Sensor 与统计

讲清 Bayer CFA、四种 pattern、R/Gr/Gb/B 分离、active area、orientation、black/white level、bit depth、饱和、直方图、分位数和 ROI。

补强 shot noise、read noise、quantization noise、signal-dependent noise 和动态范围近似估计。提供小型 Bayer 矩阵手算示例。若现有数据支持，补 mean-variance/mean-std、metadata 对比和 ROI 选择失败案例。

### Week 2：BLC、DPC、LSC

- BLC：黑电平来源、per-channel black level、归一化、误差传播。尽量完成 `black_level + (-10, 0, +10)` 实验。
- DPC：hot/dead/stuck pixel、静态 defect map、动态检测、同色邻域、MAD、precision/recall、强边缘误检。优先利用 `exercises/week2_dpc_injection.py` 完成人工坏点注入验证。
- LSC：光学暗角、PRNU、color shading、径向模型、四通道 gain、mesh LUT、噪声放大和真实 flat-field 标定流程。

### Week 3：Demosaic 与 AWB

用位置公式和小矩阵讲清 bilinear demosaic，建立 zipper、false color、moire、blur 的局部 crop 检查框架。严格区分 Bilinear、Malvar-He-Cutler、edge-aware、AHD 和学习式方法。

AWB 讲清 Gray World、White Patch、Gray ROI、线性域、gain 与噪声/饱和的关系，以及大面积单色、蓝天、混合光源、低光和镜面高光失败案例。不能只用全图 R/G、B/G 接近 1 判断效果。

### Week 4：CCM、Tone Mapping、Gamma/OETF

讲清 camera RGB、XYZ、Lab、3×3 CCM、矩阵方向、负值和色域外处理。明确 metadata CCM 与 ColorChecker 标定 CCM 的区别。

区分 Tone Mapping 和 Gamma，解释 percentile、Reinhard、S-curve、power gamma 与标准 sRGB OETF。增加曲线、数值例子和 hard clip/soft clip/色相保持压缩讨论。

### Week 5：IQA、消融与参数敏感性

讲清 PSNR、SSIM、MAD、DeltaE 的前提和局限，说明配准、颜色空间和 range 一致的重要性。建立 center/corner/dark/highlight/texture/edge 等统一 ROI 框架。

区分模块开关消融和参数扫描。优先选择 DPC `mad_k`、LSC strength、tone strength 做少量有假设的敏感性实验。

### Week 6：综合验收

把 Week 6 重组为阶段毕业实验，串联 static/dynamic DPC、synthetic mesh LSC、多种 demosaic baseline、三类 AWB、CCM/DeltaE 边界、OETF/S-curve、ROI IQA 和完整故障诊断。

明确尚未完成的产品级能力：真实 flat-field、ColorChecker、dark-frame calibration、视频时序稳定性、性能和内存、RAW denoise、false color suppression、sharpening。

## 五、先进方法对比

允许检索外部资料，但优先使用原始论文、标准、官方文档、作者项目和源码。建议对比 OpenISP、Infinite-ISP、rawpy/LibRaw、OpenCV、Malvar-He-Cutler、AHD、经典 AWB、ColorChecker CCM、sRGB/CIE 标准、HDR+ 与可控相机流水线。

每项对比必须回答：

- 解决什么问题；
- 核心思想；
- 相比当前 baseline 的优势；
- 代价和新增数据需求；
- 是否适合阶段 1 实现；
- 如果不实现，学习者应掌握到什么程度。

不要只罗列名字。

## 六、文档组织

形成三层结构：

1. `README` 和 `materials/stage1_start_here.md`：唯一学习入口。
2. `reports/week1/` 至 `week6/`：每周 summary 负责主线，模块报告负责深入原理和实验。
3. `reports/stage1_report.md`：完整 pipeline、代表性证据、跨模块理解、故障诊断、产品级差距和阶段验收。

更新 `reports/README.md`。发现重复报告时先判断用途，再合并、标记归档或修正导航，不得无依据删除用户文件。

所有关键图必须解释输入、参数、关注区域、现象、原因、支持的结论和证据边界。全部样张可保留结果表，但只选 3～5 张代表性样张深入分析，避免机械重复。

## 七、学习闭环与验收

每个模块加入少量高价值练习：

- 运行前预测；
- 小矩阵手算；
- 关键函数独立实现；
- 参数实验；
- 根据伪影定位模块；
- baseline 与高级方法对比；
- 说明证据边界。

最终项目要求学习者对一张未见 DNG 完成 metadata 检查、基础模块复现、完整 pipeline、中间结果保存、参数实验、失败案例、参考渲染比较和限制说明。

最终验收：

- 新学习者能从唯一入口开始；
- 每周有目标、步骤、产物和验收；
- 所有路径、命令、参数与仓库一致；
- 重要结论均有证据；
- 报告、代码和实验不存在明显矛盾；
- 测试通过，或记录无法运行原因；
- 学习者完成后能独立实现和讲解基础 Soft-ISP。

## 八、执行和最终汇报

按以下顺序执行：

1. 审查仓库并建立证据对应表；
2. 输出简短审查结论和拟修改文件；
3. 运行测试与代表性 pipeline；
4. 修正文档—代码不一致；
5. 补少量关键实验；
6. 教程化改造分周报告；
7. 更新总报告、入口和导航；
8. 检查链接、图片、命令、术语；
9. 再次验证。

最终汇报必须包含：

- 修改文件及其解决的问题；
- 新增实验及真实结果；
- 执行命令和测试结果；
- 新的学习顺序；
- 仍缺少的真实标定证据；
- 下一步最值得补的三项内容；
- 发现的代码、报告和实验不一致。

