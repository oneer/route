# ?????????????????????? ISP ???????????
"""配置驱动 pipeline 的冒烟测试，确认 YAML 可加载且阶段开关结构有效。

中文注释说明：本文件的注释侧重解释数据流、算法意图和实验用途；除注释/docstring 外不改变运行逻辑。
"""

# ???????????????????????????
from __future__ import annotations

# ????????????????????????
import tempfile
# ????????????????????????
import unittest
# ???????????????????????????
from pathlib import Path

# ???????????????????????????
from soft_isp.pipeline import load_config


# 中文注释：ConfigTests 类封装一个 ISP 处理阶段或测试场景，实例方法负责具体计算流程。
# ????? ConfigTests ??????? test_config ?????????
class ConfigTests(unittest.TestCase):
    # 中文注释：test_default_config_has_required_sections 负责本文件中的一个处理步骤；阅读时重点关注输入数组形状、输出结构和副作用。
    # ????? test_default_config_has_required_sections ???????? ISP ????????????
    def test_default_config_has_required_sections(self) -> None:
        # ???????? config?????????????
        config = load_config(Path(__file__).parents[1] / "configs" / "default.yaml")
        # ?????????????????????? ISP ???????????
        self.assertIn("pipeline", config)
        # ?????????????????????? ISP ???????????
        self.assertIn("parameters", config)
        # ?????????????????????? ISP ???????????
        self.assertIn("output", config)

    # 中文注释：test_non_mapping_config_is_rejected 负责本文件中的一个处理步骤；阅读时重点关注输入数组形状、输出结构和副作用。
    # ????? test_non_mapping_config_is_rejected ???????? ISP ????????????
    def test_non_mapping_config_is_rejected(self) -> None:
        # ??????????????????????????????
        with tempfile.TemporaryDirectory() as directory:
            # ???????? path?????????????
            path = Path(directory) / "invalid.yaml"
            # ???????? path.write_text("- one\n- two\n", encoding?????????????
            path.write_text("- one\n- two\n", encoding="utf-8")
            # ??????????????????????????????
            with self.assertRaises(ValueError):
                # ?????????????????????? ISP ???????????
                load_config(path)


# ??????????????????????????????????
if __name__ == "__main__":
    # ?????????????????????? ISP ???????????
    unittest.main()
