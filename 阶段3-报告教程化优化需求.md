# 阶段 3：C++ 高性能 ISP 工程化报告教程化优化需求

## 任务目标

你是一名资深 ISP 算法工程师、C++ 性能工程师和技术教材作者。请对 `stage3_cpp_isp/` 进行系统审查、验证和教程化改造，使学习者真正掌握：

```text
算法原理
→ Python golden reference
→ C++17 实现
→ 单元/边界测试
→ Python-C++ 数值对齐
→ LUT/fixed-point
→ benchmark
→ tile/halo/线程/cache 分析
→ 完整 pipeline
```

报告不能只是完成清单或性能数字集合，而要成为“如何把图像算法做成可靠 C++ 模块”的教程。

## 一、审查范围

先阅读：

- `stage3_cpp_isp/README.md`
- `stage3_cpp_isp/CMakeLists.txt`
- `stage3_cpp_isp/include/cpp_isp/`
- `stage3_cpp_isp/src/`
- `stage3_cpp_isp/python_ref/`
- `stage3_cpp_isp/tests/`
- `stage3_cpp_isp/benchmarks/`
- `stage3_cpp_isp/tools/`
- `stage3_cpp_isp/configs/`
- `stage3_cpp_isp/data/`
- `stage3_cpp_isp/reports/`
- `study-roadmap/阶段3-C++高性能ISP工程化学习路线.md`

建立“算法公式—Python reference—C++ API/实现—测试向量—误差阈值—benchmark—报告结论”对应表。

特别检查：

- CPF32 格式说明是否与实现一致；
- width/height/channel/stride/range 约定；
- border policy；
- Python/C++ dtype 和 rounding；
- 测试是否覆盖奇数尺寸、1×1、极值、NaN/Inf（如接口允许）；
- benchmark 是否为 Release、是否 warmup、是否重复、设备信息是否完整；
- 多线程、tile、LUT 等性能结论是否有实际实现支撑；
- `stage3_report.md`、alignment/performance/算法报告是否重复或缺少证据。

## 二、事实和修改边界

- 不得编造 SIMD、线程加速、4K 实时性、cache 命中或硬件结果。
- 没有 perf/VTune/Nsight 等证据时，只能把瓶颈写成基于复杂度和时间缩放的推断。
- Python reference 是项目 golden reference，不等于算法理论真值。
- HDR 当前是 aligned toy merge，不得描述成完整 HDR、运动配准或去鬼影。
- small NLM reference 不得描述成高性能产品实现。
- MinGW/本机结果不能无条件泛化到 ARM、NEON 或移动端。
- 只做与教程正确性、验证和必要实验直接相关的最小代码修改。

## 三、统一模块教程模板

每个 C++ 模块必须讲清：

1. 算法问题和公式；
2. 输入输出、layout、dtype、range；
3. Python reference 为什么这样写；
4. C++ API 设计；
5. 循环顺序、索引和 border；
6. 复杂度与内存访问量；
7. Python/C++ 易不一致点；
8. 测试向量为什么这样设计；
9. 误差指标和阈值来源；
10. benchmark 方法；
11. 优化前后的结果与正确性复验；
12. 质量、误差、速度、内存的 tradeoff；
13. 产品级升级方向；
14. 调试练习和面试复述。

代码讲解采用小图、小矩阵和地址/索引示例，避免只贴大段源码。

## 四、分周补强

### Week 0～1：工程骨架、Image 与 Border

系统讲解 CMake target、Debug/Release、sanitizer、GoogleTest/Benchmark、CPF32 二进制布局和跨语言验证。

用示意图解释 row-major、planar/interleaved、width/height/stride/channel、owning buffer/view、连续与非连续内存。补 1×1、奇数尺寸、stride>width 和边界映射手算例子。

### Week 2～4：噪声、去噪和性能

讲清 Poisson-Gaussian 模型、Gaussian/box/bilateral 公式、kernel、sigma spatial/range、同一参数在不同 range 下的意义。

对 bilateral 至少解释 direct exp、range LUT、rows/tile/thread 版本的实际差别。性能分析包含：

- 理论复杂度；
- 每像素邻域访问；
- 计算与访存；
- tile 和 halo；
- 线程拆分；
- speedup 与 efficiency；
- 1080P/4K 时间缩放；
- 优化后对齐误差。

如果仓库没有真正实现某条优化，改为“建议实验”，不得写成完成。

真实 SIDD bridge 要说明输入已经处于什么域，不能称为真实 RAW denoise。

### Week 5：Global Tone Mapping

讲清 Reinhard、Filmic、S-curve、percentile 的公式、参数、单调性和边界。区分 RGB per-channel 与 luminance-based 方法，分析 hue/saturation/clipping。

增加曲线图、梯度输入和高光/暗部/中间调 ROI。解释为什么某些算法发灰或压缩过强。

### Week 6：LUT 与 Fixed-Point

从 bit depth、Q format、scale、round、shift、saturate 和 overflow 开始讲。用数值例子演示 float→index→LUT output→fixed output。

误差报告至少包含 max/mean/RMSE/PSNR、failed count、梯度 banding 和暗部误差。解释 LUT size 与 cache、速度、精度的关系；性能结论必须来自 benchmark。

### Week 7：Local TM 与 HDR Toy

讲清 base/detail 分解、reconstruction、halo 产生位置和 bilateral/box base 的差别。

HDR toy 讲清 short/long exposure、饱和权重、暗部权重、曝光比例、对齐假设和 merge 后为何仍需 Tone Mapping。建立 motion/ghosting 反例说明当前边界。

### Week 8：Pipeline 集成

讲清模块开关、数据契约、中间结果、配置、错误传播和端到端 benchmark。加入“输出异常时从哪个中间张量开始排查”的流程。

总报告不能只链接分报告，要提炼：

- 正确性闭环；
- 代表性算法结果；
- 数值近似；
- 性能瓶颈；
- 产品级限制；
- 阶段 4 接口基准。

## 五、先进工程方法对比

优先使用 C++ 标准、GoogleTest/Benchmark 官方文档、OpenCV 文档、经典论文和硬件厂商优化指南。

对比：

- scalar、LUT、SIMD、线程、tile；
- OpenCV `parallel_for_`/Universal Intrinsics；
- Halide、OpenCL/CUDA、NEON/AVX 的适用位置；
- bilateral grid、guided filter、NLM/BM3D；
- Reinhard、Filmic、local TM；
- float、fixed-point、LUT；
- CPU library 与硬件 ISP streaming pipeline。

每项说明收益、复杂度、数值风险、可移植性和阶段 3 是否值得实现。不要为了“高级”加入未验证的 AVX/NEON 代码。

## 六、实验和报告规范

所有性能结果必须注明：

- CPU、核心数、编译器和版本；
- Build type 和关键 flags；
- 输入尺寸、通道、算法参数；
- 线程数；
- warmup、重复次数和统计量；
- 是否包含 I/O、分配和转换；
- latency 还是 throughput。

所有对齐结果必须注明：

- reference 和 candidate；
- dtype/range；
- border；
- tolerance；
- max/mean/RMSE/PSNR/failed count；
- 差异集中区域。

选代表性图片和 synthetic vector 深入分析，避免堆表。性能优化前后都必须重跑 correctness。

## 七、学习闭环

每周加入：

- 先写 Python 再移植的练习；
- 手算索引/border/fixed-point；
- 故意制造 stride、RGB/BGR、rounding、border bug；
- 用 compare tool 定位首个错误阶段；
- 预测 kernel/tile/thread 改动影响；
- 解释 benchmark 陷阱。

最终 capstone 要求学习者在干净目录独立实现一个小模块，完成 reference、API、测试、对齐、benchmark 和报告。

## 八、验收与汇报

实际配置、构建并运行测试；能运行时执行代表性 benchmark 和 Python/C++ 对齐。不得只看已有 CSV。

最终达到：

- 所有核心模块有清晰数据契约；
- Python/C++ 对齐方法可复用；
- 误差阈值有解释；
- 性能数字可复现；
- 优化没有破坏正确性；
- 学习者能解释 stride、border、tile、halo、fixed-point、cache 和线程 tradeoff；
- 清楚区分学习版 C++ 库与产品级实时 ISP。

最终汇报修改文件、修复的不一致、运行命令、测试和 benchmark、不能复现的结果、教程新顺序、尚未具备的 SIMD/ARM/真实硬件证据，以及下一步三项优先改进。

