# 阶段 4：端侧 AI-ISP 部署与 CUDA 推理报告教程化优化需求

## 任务目标

你是一名资深 AI 推理部署工程师、AI-ISP 算法工程师、CUDA/C++ 性能工程师和技术教材作者。请对 `stage4_deploy_isp/` 进行系统审查、验证和教程化改造。

最终教程必须让学习者理解并能复现：

```text
固定 PyTorch baseline
→ ONNX 导出与图检查
→ ONNX Runtime Python 对齐
→ ONNX Runtime C++ 推理
→ TensorRT/高性能后端 FP32/FP16
→ INT8 PTQ/QDQ 与校准
→ CUDA 前后处理
→ latency breakdown 与 profiling
→ 画质/速度/内存权衡
```

重点不是“成功转出模型”，而是正确性、性能测量、量化画质和工程复现。

## 一、先审查

阅读：

- `stage4_deploy_isp/README.md`
- `stage4_deploy_isp/reports/`
- `stage4_deploy_isp/scripts/`
- `stage4_deploy_isp/deploy/`
- `stage4_deploy_isp/cpp/`
- `stage4_deploy_isp/cuda_kernels/`
- `stage4_deploy_isp/configs/`
- `stage4_deploy_isp/models/`
- `stage4_deploy_isp/data/test_inputs/`
- `stage4_deploy_isp/outputs/`
- `stage4_deploy_isp/CMakeLists.txt`
- `study-roadmap/阶段4-端侧AI-ISP部署与CUDA推理学习路线.md`

建立“模型/checkpoint—ONNX—backend/engine—输入 manifest—输出—误差 CSV—latency 日志—报告结论”对应表。

重点检查：

- 当前实际部署的是哪个模型；
- 输入 shape/layout/RGB 顺序/range/normalization；
- PyTorch、ORT Python、ORT C++、TensorRT、INT8 是否使用相同输入；
- ONNX `.data` 外部权重依赖；
- TensorRT engine 的 GPU/版本绑定；
- benchmark 是否区分 warmup、同步和 I/O；
- INT8 模型、校准数据和评价结果是否真正对应；
- `week5_mobile_inference.md` 是否有真实设备证据；
- CUDA preprocess 是否真正接入端到端流程；
- 报告中的目标与仓库已完成能力是否一致。

## 二、事实和安全边界

- 不得编造 GPU、Android/ARM 设备、功耗、内存、TensorRT engine 或 latency。
- TensorRT engine 通常与 TensorRT/CUDA/GPU 架构相关，必须记录环境，不能承诺通用。
- 没有真实 Android 设备结果时，不得声称完成移动端部署。
- ORT QDQ INT8、TensorRT INT8 和动态量化必须区分。
- 图像恢复模型的量化不能只看 max error；必须评价输出画质和失败区域。
- `trtexec` 结果、ORT session latency、C++ end-to-end latency 不得混在同一口径。
- 网络访问或依赖不可用时记录原因，不伪造验证。
- 修改聚焦教程、复现和关键缺口，不随意更换模型或大改架构。

## 三、统一教学模板

每个部署步骤至少包含：

1. 为什么需要这一步；
2. 输入输出协议；
3. 工具/运行时在链路中的角色；
4. 核心概念和 API；
5. 对应脚本/C++ 文件；
6. 运行命令和环境要求；
7. 正确输出；
8. 对齐指标和阈值；
9. 常见失败及排查顺序；
10. 性能测量方法；
11. 画质、速度、显存/内存 tradeoff；
12. 当前证据边界；
13. 练习与掌握标准。

用张量 shape 表和数据流图贯穿全过程，明确 NCHW/NHWC、RGB/BGR、float32/float16/int8、host/device 和 clamp/round。

## 四、分周补强

### Week 0：固定 baseline

讲清为什么部署阶段必须冻结模型、checkpoint、测试集和预处理。建立 model card 和 deployment contract：

- 模型来源和结构；
- checkpoint hash（可可靠生成时）；
- 输入输出名称、shape、dtype、range；
- clamp/residual 行为；
- 固定测试 manifest；
- PyTorch 画质和 latency baseline；
- 设备与软件版本。

### Week 1：ONNX 导出与对齐

讲清 `eval()`、`no_grad()`、dummy input、opset、fixed/dynamic shape、constant folding、external data、checker、shape inference、Netron 和 simplifier。

对齐必须在相同输入上报告 max/mean/RMSE/PSNR，并保存 error map。解释浮点误差、算子语义和动态 shape 常见问题。

优先使用当前 PyTorch 推荐导出路径和官方文档；如果保留旧 exporter，说明版本原因。

### Week 2：ONNX Runtime C++

讲清 Session、Execution Provider、allocator、tensor ownership、input/output names、NCHW 内存和 image I/O。

逐步对齐：

```text
Python preprocess tensor
→ C++ input tensor
→ ORT Python output
→ ORT C++ raw output
→ C++ postprocess image
```

避免最终 PNG 掩盖 tensor 级错误。解释线程、graph optimization 和 cold/warm latency。

### Week 3：TensorRT FP32/FP16

如果仓库实际支持 TensorRT，讲清 ONNX parser、builder、optimization profile、engine、execution context、workspace、stream、H2D/D2H 和 `trtexec`。

统一比较 ORT CPU、ORT CUDA/TensorRT EP、`trtexec` 和 engine 时，必须注明口径。FP16 报告同时给数值误差、PSNR/SSIM 和失败 crop。

当前只有日志或 engine、没有可重复构建环境时，应写成“已有历史证据”，并提供重新构建指南。

### Week 4：INT8

讲清 PTQ、静态量化、QDQ、scale/zero-point、per-tensor/per-channel、symmetric/asymmetric、calibration method 和代表性数据。

审查校准集是否与测试集隔离、是否覆盖暗部/高光/纹理/不同噪声。报告：

- 模型大小；
- latency；
- FP32/INT8 输出误差；
- PSNR/SSIM drop；
- 每样张最差案例；
- error map/crop；
- 可接受性结论。

阈值只能作为项目标准，不得包装为行业统一标准。

### Week 5：移动端路径

根据仓库事实决定：

- 有真实设备：记录型号、SoC、OS、后端、线程、CPU/GPU/Vulkan、内存和温度条件。
- 无真实设备：明确这是调研/设计章节，不能写成已完成。

对比 NCNN、MNN、TFLite、OpenVINO、TensorRT 的目标平台、模型转换、算子支持、量化和工程代价。只选择与求职目标相关的一条作为后续实践。

### Week 6：CUDA 前后处理与 Pipeline Profile

讲清 CPU preprocess、H2D、GPU kernel、inference、D2H、postprocess 和 save 的边界。

对 `pack_raw.cu`、`normalize.cu` 和 C++ benchmark 逐步解释 grid/block、线程索引、边界、coalescing、pinned/pageable memory、同步、CUDA event。

确认 CUDA preprocess 是否和当前 RGB 模型输入真正匹配。未接入完整 pipeline 时不要声称端到端加速。

latency 报告至少包含平均、p50/p90（样本数足够时）、warmup、重复次数、同步点以及是否包含 I/O。分析“kernel 更快但端到端无改善”的原因。

## 五、先进方法与当前生态

部署工具版本变化较快，必须检索最新官方文档并标注版本/访问日期。优先使用 PyTorch、ONNX、ONNX Runtime、NVIDIA TensorRT/CUDA/Nsight、NCNN/MNN 官方资料。

对比时回答：

- 目标硬件；
- 支持精度；
- 动态 shape；
- 算子兼容；
- 图优化；
- 量化流程；
- 调试工具；
- 性能与可移植性；
- 当前项目选择该后端是否合理。

可补充 Polygraphy、ONNX GraphSurgeon、TensorRT Model Optimizer、Nsight Systems/Compute 等工具，但只有实际使用后才能写进完成成果。

## 六、正确性与性能协议

建立统一 correctness matrix：

| Backend | Precision | Input | Reference | max/mean/RMSE | PSNR/SSIM | 状态 |
|---|---|---|---|---|---|---|

建立统一 latency matrix：

| Backend | Device | Shape | Precision | Warmup/Runs | Pre | H2D | Infer | D2H | Post | E2E |
|---|---|---|---|---|---:|---:|---:|---:|---:|---:|

同一表中不得混用不同设备、shape、batch 或计时口径。GPU 测时必须说明同步；模型加载/engine build 应与 steady-state inference 分开。

## 七、文档和学习闭环

README 作为唯一入口，分周报告讲原理与实验，总报告讲完整系统、证据、tradeoff 和岗位表达。更新 reproducibility checklist，使新环境可以核对：

- 模型和输入文件；
- Python/C++ 依赖；
- CUDA/TensorRT/ORT 版本；
- 构建命令；
- smoke test；
- 预期输出；
- 对齐阈值；
- benchmark 口径。

每周加入调试练习：

- RGB/BGR 错误；
- NCHW/NHWC 错误；
- `[0,1]`/`[0,255]` 错误；
- FP16 溢出；
- dynamic shape/profile 错误；
- 未同步导致虚假 latency；
- calibration 分布偏差；
- PNG 量化掩盖 tensor error。

最终 capstone 要求学习者从固定 checkpoint 独立完成 ONNX、ORT Python/C++ 对齐、至少一种加速或量化、端到端拆时和失败分析。

## 八、验收和最终汇报

在当前环境允许范围内运行：

- PyTorch baseline；
- ONNX checker/ORT alignment；
- C++ 构建与 smoke test；
- 后端 benchmark；
- INT8 评价；
- CUDA preprocess benchmark。

无法运行 GPU/移动端内容时，保留历史证据并明确“未在当前环境复验”。

最终达到：

- 输入输出协议唯一且明确；
- 各后端使用同一固定输入；
- 正确性和性能口径统一；
- FP16/INT8 有画质证据；
- engine/模型的环境依赖被记录；
- 真实设备与桌面模拟被明确区分；
- 学习者能系统排查输出异常和 latency 异常；
- 能把阶段 1～4 串成完整 AI-ISP 工程故事。

最终汇报修改文件、验证命令、各后端状态、对齐和性能结果、未复验内容及原因、真实设备/功耗/移动端缺口、教程学习顺序，以及下一步最值得完成的三项工作。

