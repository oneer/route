# 阶段 2：AI-ISP 图像恢复报告教程化优化需求

## 任务目标

你是一名资深 AI-ISP/图像恢复算法工程师、PyTorch 工程师和中文技术教材作者。请对 `stage2_ai_isp/` 进行系统审查、实验核验和教程化改造。

目标不是增加篇幅或追逐 SOTA，而是让学习者完整掌握：

```text
任务与数据域定义
→ 退化和 GT 分析
→ Dataset/DataLoader
→ 训练闭环
→ DnCNN/UNet/NAFNet-lite
→ Loss/Metric
→ 公平实验与可复现
→ Pseudo RAW/低光任务
→ Failure case
→ ONNX/C++ smoke test
```

## 一、先审查再修改

必须阅读：

- `stage2_ai_isp/README.md`
- `stage2_ai_isp/stage2_start_here.md`
- `stage2_ai_isp/reports/`
- `stage2_ai_isp/ai_isp/`
- `stage2_ai_isp/configs/`
- `stage2_ai_isp/scripts/`
- `stage2_ai_isp/tests/`
- `stage2_ai_isp/exercises/`
- `stage2_ai_isp/deployment/`
- `study-roadmap/阶段2-AI-ISP图像恢复学习路线.md`

重点审查 `stage2_learning_flow.md`、`stage2_upgrade_plan.md`、`stage2_weekly_upgrade_from_reports.md`、`stage2_final_project_report.md`、`stage2_engineering_summary.md`、评估/公平性协议和 Week0～12 报告。

建立“报告结论—配置—run/checkpoint—指标 CSV—可视化—代码—测试”证据表。识别重复报告、自动生成报告、手写教程和真实实验结果之间的边界。

## 二、事实和范围

- 不得编造训练结果、checkpoint、GPU、耗时或数据规模。
- `runs/` 不存在时不能声称重新训练成功。
- SIDD tiny 结果不能冒充官方 benchmark。
- sRGB paired denoise、pseudo RAW/RGGB 和真实 sensor RAW 必须严格区分。
- synthetic low-light 不得描述成真实低光采集。
- ONNX Runtime C++ smoke test 不得描述成量产端侧部署。
- 修改以教程和验证为主，不盲目增加 Restormer、Diffusion 等大模型。
- 只有为复现实验、修复不一致或补关键评价时才最小修改代码。

## 三、统一教程模板

每周至少回答：

1. 本周解决什么问题，为什么现在学；
2. 输入域、输出域和 GT 从哪里来；
3. 数据如何划分，是否可能泄漏；
4. 模型每个关键模块在做什么；
5. loss 为什么这样选；
6. 训练循环中 forward、backward、optimizer、scheduler、validation 的关系；
7. 配置参数的意义和影响；
8. 如何复现实验；
9. baseline、模型输出和 GT 如何比较；
10. 指标、三联图、error map 和 crop 怎么读；
11. 结果证明什么、不证明什么；
12. 失败属于数据、退化、模型、loss、训练还是部署问题；
13. 学习版和论文/产品级方法的差距；
14. 练习、自测和掌握标准。

重要概念采用“直觉→公式→张量 shape→代码→训练曲线→可视化→失败案例”讲解。

## 四、分周重点

### Week 0～1：训练基础与 Toy RGB

完整讲清 Dataset、DataLoader、batch、epoch/step、forward、loss、autograd、backward、optimizer、scheduler、train/eval、checkpoint、seed 和过拟合小样本测试。

解释 DnCNN residual learning、直接预测 clean 与预测 noise 的差别；L1/L2 对异常值、平滑和收敛的影响；patch size、batch size、学习率和 receptive field 的关系。

### Week 2：真实 paired RGB 数据

讲清 SIDD 数据来源、noisy/GT 配对、scene 划分、crop、augmentation、像素对齐、数据泄漏和 noisy baseline。增加 Dataset Card：来源、license、scene、相机/ISO（若可得）、数量、split、预处理、限制。

必须解释 SIDD GT 不是天然无噪真值，以及 sRGB 噪声已经经过相机 ISP。

### Week 3～5：模型、公平比较与 NAFNet

分别讲清 DnCNN、UNet、NAFNet-lite 的结构、张量变化、残差/skip、归一化、SimpleGate、SCA 和复杂度。

公平比较必须控制数据 split、训练步数、patch、增强、loss、评价脚本和 checkpoint 选择。若现有实验不完全公平，明确写出，不强行排名。

补参数量、checkpoint 大小、MACs/FLOPs（能可靠计算时）、峰值内存和 latency 的定义。指标和速度必须注明设备、线程、warmup、重复次数。

### Week 4：Loss、Metric 与可视化

讲清 L1、L2、Charbonnier、SSIM/perceptual 的优化目标与风险；PSNR、SSIM、LPIPS（如实际使用）和 ROI 指标的适用范围。

所有模型评估统一保存 noisy/output/GT、error map、代表性 crop。解释数据 range、颜色空间、边界 crop 和图像配准对指标的影响。

### Week 6：Pseudo RAW/ISP Bridge

严格说明 pseudo RAW 的生成过程、Bayer pack、4 通道含义、空间分辨率变化、black level/white level、linear/sRGB 问题和 domain gap。

将阶段 1 的 ISP 知识映射到 unprocessing：inverse OETF、inverse tone、inverse CCM、inverse WB、mosaic、noise。当前只实现了其中一部分时必须如实标注。

对比 RGB denoise 与 RAW-like denoise，但不得用不公平结果下结论。

### Week 7：低光增强

讲清曝光缩放、shot/read noise、颜色和动态范围。明确 synthetic low-light 的退化公式、参数和随机种子。

除 PSNR/SSIM 外，分析亮度恢复、暗部噪声、颜色偏移、高光 clipping 和过度增亮。将“增强”和“降噪”目标区分开。

### Week 8～9：失败案例与项目总结

建立 failure taxonomy：

- 残余亮噪/色噪；
- 过平滑与纹理损失；
- hallucination/伪纹理；
- 色偏；
- 边缘/棋盘格；
- 高光异常；
- domain shift；
- 量化或部署误差。

每个案例必须包含 crop、指标、原因假设、验证实验和下一步，而不是只贴图。

### Week 10～12：工程总结、ONNX 与 C++

讲清模型输入输出协议、NCHW、dtype/range、eval/no_grad、opset、dynamic shape、PyTorch/ONNX/ORT C++ 对齐、max/mean error、PSNR 和 latency 测量。

明确这是部署基线，为阶段 4 铺路，不重复扩展完整 TensorRT/INT8 教程。

## 五、先进方法对比

优先查原论文和官方代码，对比 DnCNN、UNet、NAFNet、Restormer、MPRNet、SwinIR、SID、Unprocessing、CycleISP 和真实噪声建模。每项说明：

- 任务和数据域；
- 结构或数据方法的核心；
- 相比当前 baseline 的收益；
- 训练/显存/部署代价；
- 是否适合当前数据规模；
- 阶段 2 应实现、选读还是留到后续。

重点强调：数据与退化建模的重要性通常不低于换网络。

## 六、文档和学习闭环

以 `stage2_start_here.md` 为唯一入口。周报负责教程主线，自动导出的评估报告负责保存事实证据，总报告负责跨周总结。清理导航和重复叙述，但不删除仍有历史价值的文件。

每周加入：

- 运行前预测；
- overfit 1～5 张图的调试练习；
- shape 跟踪；
- 配置修改实验；
- 指标与主观冲突分析；
- failure case 归因；
- 独立实现任务。

最终 capstone 要求学习者独立完成一个新配置：数据检查、baseline、训练、评估、可视化、失败案例、工程指标、ONNX 导出和项目边界说明。

## 七、验收和最终汇报

必须验证测试、代表性评估脚本和已有 ONNX/C++ smoke test；大规模训练无法重跑时检查 checkpoint、配置和现有产物的一致性。

最终达到：

- 学习者能从零搭建可复现训练闭环；
- 能解释 SIDD、synthetic、pseudo RAW 的差别；
- 能公平比较模型/loss；
- 能从失败案例反推问题；
- 能把阶段 1 的传统 ISP 与阶段 2 的 AI 模型连接；
- 报告中的指标、设备、数据和命令均可核查。

最终汇报修改文件、补充实验、运行命令、测试结果、教程新顺序、无法重现实验及原因、仍存在的数据/算力/真实 RAW 缺口，以及最值得继续补的三项内容。

